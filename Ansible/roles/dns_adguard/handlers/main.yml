---
# Restart AdGuard Home after its managed configuration changes.
# Перезапуск AdGuard Home после изменения управляемой конфигурации.
- name: Restart AdGuard Home
  ansible.builtin.systemd:
    name: "{{ dns_adguard_service_name }}"
    state: restarted
