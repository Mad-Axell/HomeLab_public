---
# =============================================================================
# AdGuard Home - Debian Dependencies
# AdGuard Home - Зависимости Debian
# =============================================================================

- name: Install AdGuard Home system dependencies (Debian)
  ansible.builtin.package:
    name:
      - libcap2-bin
      - python3-pip
    state: present
