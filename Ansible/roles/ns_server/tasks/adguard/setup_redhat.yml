---
# =============================================================================
# AdGuard Home - RedHat Dependencies
# AdGuard Home - Зависимости RedHat
# =============================================================================

- name: Install AdGuard Home system dependencies (RedHat)
  ansible.builtin.package:
    name:
      - libcap
      - python3-pip
    state: present
