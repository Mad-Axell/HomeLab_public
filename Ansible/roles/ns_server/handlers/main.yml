---
# Restart the DNS server after its validated configuration changes.
# Перезапуск DNS-сервера после изменения проверенной конфигурации.
- name: Restart Unbound DNS server
  ansible.builtin.systemd:
    name: "unbound"
    state: restarted
