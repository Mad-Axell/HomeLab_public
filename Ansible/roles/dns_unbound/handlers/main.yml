---
# Restart Unbound after its validated configuration changes.
# Перезапуск Unbound после изменения проверенной конфигурации.
- name: Restart Unbound
  ansible.builtin.systemd:
    name: "{{ dns_unbound_service_name }}"
    state: restarted
