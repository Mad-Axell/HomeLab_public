---
# Основной файл роли безопасности - включает все задачи по защите системы

# Задачи по защите ядра Linux
- include_tasks: linux-kernel_protection.yaml

# Защита пользовательского пространства
- include_tasks: user_space_protection.yaml

# Сокращение атакуемой поверхности
- include_tasks: reducing_the_attack_perimeter.yaml

# Общая защита ОС
- include_tasks: harden_os.yaml

# Управление правами доступа к файловой системе
- include_tasks: access_rights_to_file_system_objects.yaml